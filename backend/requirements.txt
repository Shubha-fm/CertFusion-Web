fastapi==0.115.8
uvicorn[standard]==0.34.0
python-multipart==0.0.20
numpy==2.2.3
Pillow==11.1.0
scipy==1.15.2
nibabel==5.3.2
z3-solver==4.13.4.0
pydantic==2.10.6
reportlab==4.3.1

# PyTorch is intentionally not installed in the default free CPU/demo deployment.
# The application runs in research-demo mode when no model checkpoint is present.
# For paper-checkpoint deployment, install a CPU-compatible PyTorch build separately
# and provide backend/model_artifacts/certfusion.pt.
